# =====================================================================
# Dependencias de desarrollo y pruebas.
# Solo se instalan en local o en CI; no entran en las imágenes Docker.
# =====================================================================

pytest==8.3.4
httpx==0.28.1            # Cliente HTTP para testear la API de FastAPI
ruff==0.8.2              # Linter y formateador
